"""Pruebas.

Es un paquete a proposito: sin `__init__.py`, las importaciones entre ficheros
de prueba funcionan con `python -m pytest` y fallan con `pytest`, o al reves.
En el proyecto anterior eso paso la maquina de desarrollo y lo caza el CI.
"""
